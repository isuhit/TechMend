<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - Page Not Found</title>

    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

    <style>
      /* Import Font */
      @import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap");

      /* Base Styles & Reset */
      body,
      html {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Inter", sans-serif;
        height: 100%;
      }

      /* * Change: We removed the 'display: flex' from body
         * and applied it to the main content wrapper instead
         * to allow the header to sit above the main content.
         */
      body {
        background-color: #f3f4f6; /* Equivalent to bg-gray-100 */
        min-height: 100vh;
      }

      /* New Wrapper for Centering the Card Below the Header */
      .main-content-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
        /* Occupy the remaining vertical space */
        min-height: calc(
          100vh - 4rem
        ); /* 100vh minus the typical header height (h-16) */
      }

      /* Error Card */
      .error-card {
        text-align: center;
        background-color: #ffffff; /* Equivalent to bg-white */
        padding: 2rem; /* Equivalent to p-8 */
        border-radius: 1rem; /* Equivalent to rounded-2xl */
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
          0 4px 6px -2px rgba(0, 0, 0, 0.05); /* Equivalent to shadow-lg */
        max-width: 28rem; /* Equivalent to max-w-md */
        width: 100%;
      }
      /* ... (rest of your existing styles) ... */

      .icon-container {
        margin-left: auto;
        margin-right: auto;
        margin-bottom: 1.5rem; /* Equivalent to mb-6 */
        height: 5rem; /* Equivalent to h-20 */
        width: 5rem; /* Equivalent to w-20 */
        color: #3b82f6; /* Equivalent to text-blue-500 */
      }
      .icon-container svg {
        width: 100%;
        height: 100%;
      }
      .error-code {
        font-size: 3.75rem; /* Equivalent to text-6xl */
        font-weight: 700; /* Equivalent to font-bold */
        color: #1f2937; /* Equivalent to text-gray-800 */
      }
      .error-title {
        font-size: 1.5rem; /* Equivalent to text-2xl */
        font-weight: 600; /* Equivalent to font-semibold */
        color: #374151; /* Equivalent to text-gray-700 */
        margin-top: 0.5rem; /* Equivalent to mt-2 */
      }
      .error-message {
        color: #6b7280; /* Equivalent to text-gray-500 */
        margin-top: 1rem; /* Equivalent to mt-4 */
      }
      .action-button-container {
        margin-top: 2rem; /* Equivalent to mt-8 */
      }
      .action-button {
        display: inline-block;
        border-radius: 0.5rem; /* Equivalent to rounded-lg */
        background-color: #2563eb; /* Equivalent to bg-blue-600 */
        padding: 0.75rem 1.5rem; /* Equivalent to px-6 py-3 */
        font-size: 0.875rem; /* Equivalent to text-sm */
        font-weight: 600; /* Equivalent to font-semibold */
        color: #ffffff; /* Equivalent to text-white */
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
          0 2px 4px -1px rgba(0, 0, 0, 0.06); /* Equivalent to shadow-md */
        transition: background-color 0.2s ease-in-out;
        text-decoration: none;
      }
      .action-button:hover {
        background-color: #1d4ed8; /* Equivalent to hover:bg-blue-700 */
      }

      /* Responsive Styles for smaller screens */
      @media (min-width: 640px) {
        .error-card {
          padding: 3rem; /* Equivalent to sm:p-12 */
        }
      }
    </style>
  </head>
  <body>
    <header class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <div class="flex-shrink-0">
            <a
              href="/"
              class="text-2xl font-extrabold text-indigo-600 tracking-wider"
            >
              TechMend
            </a>
          </div>

          <nav class="hidden sm:ml-6 sm:flex sm:space-x-8">
            <a
              href="/#how-it-works"
              class="text-gray-900 hover:bg-gray-50 px-3 py-2 rounded-md text-sm font-medium transition duration-150 ease-in-out"
            >
              How it works
            </a>
            <a
              href="/contact"
              class="text-gray-500 hover:bg-gray-50 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition duration-150 ease-in-out"
            >
              Contact Support
            </a>

            <a
              href="/request-repair"
              class="text-gray-500 hover:bg-gray-50 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition duration-150 ease-in-out"
            >
              Request Repair</a
            >
          </nav>

          <div class="-mr-2 flex items-center sm:hidden">
            <button
              type="button"
              class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-500"
              aria-controls="mobile-menu"
              aria-expanded="false"
            >
              <svg
                class="block h-6 w-6"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
                aria-hidden="true"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M4 6h16M4 12h16M4 18h16"
                />
              </svg>
            </button>
          </div>
        </div>
      </div>
      <div class="sm:hidden hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1">
          <a
            href="/"
            class="block bg-indigo-50 text-indigo-700 px-3 py-2 rounded-md text-base font-medium"
            >Home</a
          >
          <a
            href="/contact"
            class="block text-gray-700 hover:bg-gray-50 hover:text-gray-900 px-3 py-2 rounded-md text-base font-medium"
            >Contact</a
          >
        </div>
      </div>
    </header>

    <div class="main-content-wrapper">
      <div class="error-card">
        <div class="icon-container">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z"
            />
          </svg>
        </div>

        <h1 class="error-code">404</h1>
        <h2 class="error-title">Page Not Found</h2>

        <p class="error-message">
          Sorry, the page you are looking for doesn't exist or has been moved.
          Let's get you back on track.
        </p>

        <div class="action-button-container">
          <a href="/" class="action-button"> Go Back Home </a>
        </div>
      </div>
    </div>
         <!-- FOOTER -->
        <%- include("../partials/footer") %>
  </body>
</html>
